name: wearos_comp_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  watch_connectivity: ^0.2.1+1
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0
  device_info_plus: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 
flutter:
  uses-material-design: true
